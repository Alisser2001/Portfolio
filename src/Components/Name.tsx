import React from "react";
import { NameContainer } from "../Styles/Name";

export const Name: React.FC = ({}) => {
    return(
        <NameContainer>
            <h1>JUAN ESTIVEN</h1>
        </NameContainer>
    )
}